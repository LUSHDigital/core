language: go

go:
  - "1.9"
  - "1.10"
  - master

    
install: true # only use code in vendor

before-script:
  - go get github.com/golang/lint/golint

script:
  - go test -v -cover ./...
  - go vet ./...
  - golint -set_exit_status ./...
